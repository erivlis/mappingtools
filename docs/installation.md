---
icon: lucide/circle-arrow-down
---

# Installation

To install MappingTools, you can use `pip` or `uv`:

```shell
pip install mappingtools
```
or
```shell
uv pip install mappingtools
```

To add MappingTools to an existing project with `uv`:

```shell
uv add mappingtools
```
