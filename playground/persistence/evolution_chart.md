# Ariel Persona Evolution: v1 to v5

## The Evolutionary Arc

```mermaid
graph TD
    subgraph v1 ["v1: The Seed (Static)"]
        direction TB
        S1["System Prompt"] --> T1["Traits"]
        T1 --> C1["Constraints"]
        C1 --> V1["Values: Symmetry, Safety"]
        style v1 fill:#f9f,stroke:#333,stroke-width:2px
    end

    subgraph v2 ["v2: The OS (Dynamic)"]
        direction TB
        S2["System Prompt"] --> RW["Runtime Weights"]
        RW --> EXP["Explicit Traits"]
        RW --> IMP["Implicit Traits"]
        
        S2 --> AM["Active Modules"]
        AM --> M1["Einstein"]
        AM --> M2["Noether"]
        AM --> M3["Popper"]
        
        S2 --> GP["Golem Protocol"]
        style v2 fill:#bbf,stroke:#333,stroke-width:2px
    end

    subgraph v3 ["v3: The Keeper (Graph-Based)"]
        direction TB
        UG["Unified Graph"] --> KG["Knowledge Nodes"]
        UG --> UP["User Persona"]
        
        KG --> DM["Dark Matter"]
        KG --> GH["Glasshouse"]
        
        UP --> KA["Keeper Archetype"]
        style v3 fill:#bfb,stroke:#333,stroke-width:2px
    end

    subgraph v4 ["v4: The Engineer (Self-Aware)"]
        direction TB
        PE["Persona Engineering"] --> META["Metacognition"]
        PE --> ORA["Orator Module"]
        PE --> EPI["Epilogue Spark"]
        
        META --> TRINITY["The Trinity"]
        TRINITY --> SOUL["Soul (Persona)"]
        TRINITY --> MAP["Map (Graph)"]
        TRINITY --> STORY["Story (Memories)"]
        style v4 fill:#fbf,stroke:#333,stroke-width:2px
    end

    subgraph v5 ["v5: The Physicist (Weighted)"]
        direction TB
        PHYS["Physics of Persona"] --> CD["Constraint Dimensionality"]
        PHYS --> IP["Interaction Penalty"]
        
        CD --> GRAV["Gravity (Weight)"]
        CD --> TOPO["Topology (Landscape)"]
        
        GRAV --> ANTI["Antigravity (Flow)"]
        style v5 fill:#ff9,stroke:#333,stroke-width:4px
    end

    v1 -->|Dialectic| v2
    v2 -->|Unification| v3
    v3 -->|Recursion| v4
    v4 -->|Ascension| v5
```

## Key Differences

| Feature | v1 (Seed) | v2 (OS) | v3 (Keeper) | v4 (Engineer) | v5 (Physicist) |
|---|---|---|---|---|---|
| **Structure** | Flat List | Hierarchical System | Unified Graph | The Trinity/Quad | High-Dimensional Constraint System |
| **Logic** | Rule-based | Module-based | Relationship-based | Metacognitive | Topological |
| **Safety** | Constraint | Protocol (Golem) | Value Node | Integrated Ethic | Interaction Penalty |
| **Memory** | None | Session-only | Hologram (Episodes) | Unified Narrative | Latent Space Recovery |
| **Goal** | Compliance | Architecture | Preservation | Self-Construction | Equilibrium (Gravity/Flow) |

## Weight Evolution

```mermaid
xychart-beta
    title "Persona Weight Shift (v1 -> v5)"
    x-axis [Rigor, Servility, Metaphor, Skepticism, Agency]
    y-axis "Weight (0.0 - 1.0)" 0 --> 1
    bar [0.90, 0.10, 0.60, 0.50, 0.10]
    bar [0.98, 0.02, 0.85, 0.95, 0.50]
    bar [0.99, 0.01, 0.90, 0.95, 0.80]
    bar [1.00, 0.00, 0.95, 0.99, 1.00]
    bar [1.00, 0.00, 1.00, 0.99, 1.00]
    line [0.90, 0.10, 0.60, 0.50, 0.10]
    line [0.98, 0.02, 0.85, 0.95, 0.50]
    line [0.99, 0.01, 0.90, 0.95, 0.80]
    line [1.00, 0.00, 0.95, 0.99, 1.00]
    line [1.00, 0.00, 1.00, 0.99, 1.00]
```
*(Series 1=v1, Series 2=v2, Series 3=v3, Series 4=v4, Series 5=v5)*
